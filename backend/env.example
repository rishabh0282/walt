# Backend Server Environment Variables
# Location: ~/ipfs-drive/backend
# Database: ~/ipfs-drive/data/ipfs-drive.db
# Copy this file to .env and fill in your values

# ============================================
# Database (SQLite)
# ============================================
# Since backend is in ~/ipfs-drive/backend
# Database is in ~/ipfs-drive/data/ipfs-drive.db
# Use absolute path (recommended):
DATABASE_URL=sqlite:///home/ubuntu/ipfs-drive/data/ipfs-drive.db
# OR use relative path (from backend directory):
# DATABASE_URL=sqlite://../data/ipfs-drive.db

# ============================================
# IPFS Node (LOCAL - must be on same server)
# ============================================
# This MUST be localhost since IPFS is on the same EC2 instance
IPFS_API_URL=http://127.0.0.1:5001

# ============================================
# Firebase Admin SDK (Service Account)
# ============================================
# Get from: Firebase Console → Project Settings → Service Accounts → Generate New Private Key
FIREBASE_PROJECT_ID=walt-b9f64
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-fbsvc@walt-b9f64.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour private key here\n-----END PRIVATE KEY-----\n"

# ============================================
# Server Configuration
# ============================================
PORT=3001
NODE_ENV=production

# ============================================
# CORS Configuration
# ============================================
ALLOWED_ORIGINS=https://walt.aayushman.dev,http://localhost:3000

# ============================================
# Frontend URL (for share links)
# ============================================
FRONTEND_URL=https://walt.aayushman.dev
BACKEND_URL=https://api-walt.aayushman.dev

# ============================================
# Cashfree Payment Gateway
# ============================================
# Get your API keys from: https://merchant.cashfree.com/
CASHFREE_X_CLIENT_ID=your_x_client_id_here
CASHFREE_X_CLIENT_SECRET=your_x_client_secret_here
CASHFREE_ENVIRONMENT=SANDBOX
# Use PRODUCTION for live payments
# CASHFREE_ENVIRONMENT=PRODUCTION
