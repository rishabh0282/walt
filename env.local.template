# Frontend Environment Variables
# Copy this content to .env.local and fill in your values

# ============================================
# Firebase Client Configuration (REQUIRED)
# ============================================
NEXT_PUBLIC_FIREBASE_API_KEY=your_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=walt-b9f64.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=walt-b9f64
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=walt-b9f64.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789012
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789012:web:abcdef123456

# ============================================
# Backend API URL (REQUIRED)
# ============================================
NEXT_PUBLIC_BACKEND_API_URL=http://localhost:3001

# ============================================
# Pinning Service (OPTIONAL)
# ============================================
# Options: 'local', 'walt', 'pinata', 'web3storage', 'filebase', 'backend'
# 'local' or 'walt' = use self-hosted backend IPFS node (default)
# 'pinata' = use Pinata service (requires PINATA_API_KEY and PINATA_API_SECRET)
NEXT_PUBLIC_PINNING_SERVICE=local

# ============================================
# Firebase Admin SDK (REQUIRED for API routes)
# ============================================
# These are needed for Next.js API routes that verify Firebase tokens
# Get from Firebase Console → Project Settings → Service Accounts → Generate New Private Key
# Option A: Use individual fields
FIREBASE_PROJECT_ID=walt-b9f64
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@walt-b9f64.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Option B: Use JSON string (alternative to above)
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"walt-b9f64",...}

# ============================================
# IPFS Gateway (RECOMMENDED for local dev)
# ============================================
# For local development with Docker IPFS, uncomment this:
NEXT_PUBLIC_IPFS_GATEWAY=http://localhost:8080/ipfs
# 
# This ensures files are fetched from your local IPFS node instead of public gateways.
# If not set, will use backend gateway (http://localhost:3001/ipfs/) or public gateways.
